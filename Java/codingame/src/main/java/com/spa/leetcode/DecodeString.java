package com.spa.leetcode;

public class DecodeString {

    //TOBECONTINUED
    public String decodeString(String s) {
        if (s.isBlank()) {
            return s;
        }


        if (s.charAt(0) == '[') {
            //return  decodeString(s.substring(1, )) s.substring(0, s.indexOf("]"));
        }
        return "";
    }
}
